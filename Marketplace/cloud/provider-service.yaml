apiVersion: v1
kind: Service
metadata:
  name: provider-service
spec:
  selector:
    app: provider
  ports:
    - port: 3333
      targetPort: 3333
      protocol: TCP
      name: provider
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: health
  type: LoadBalancer